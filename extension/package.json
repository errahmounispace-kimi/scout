{
  "name": "scout-extension",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && bun build src/background.ts --outdir dist --target browser && bun build src/content.ts --outdir dist --target browser && bun build src/options.ts --outdir dist --target browser && cp manifest.json options.html dist/ && bun run make-icons",
    "make-icons": "bun scripts/make-icons.ts"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
